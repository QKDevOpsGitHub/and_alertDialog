language: android
sudo: false
jdk: oraclejdk8

before_install:
  - mkdir "$ANDROID_HOME/licenses" || true
  - echo -e "\n8933bad161af4178b1185d1a37fbf41ea5269c55" > "$ANDROID_HOME/licenses/android-sdk-license"
  - echo -e "\n84831b9409646a918e30573bab4c9c91346d8abd" > "$ANDROID_HOME/licenses/android-sdk-preview-license"

android:
  components:
    # Uncomment the lines below if you want to
    # use the latest revision of Android SDK Tools
    # - platform-tools
    # - tools

    # The BuildTools version used by your project
    - build-tools-25.0.2

    # The SDK version used to compile your project
    - android-25

    # Additional components
    - extra-google-google_play_services
    - extra-google-m2repository
    - extra-android-m2repository
    - addon-google_apis-google-25
    
    # Specify at least one system image,
    # if you need to run emulator(s) during your tests
    - sys-img-armeabi-v7a-android-25

  licenses:
    - 'android-sdk-preview-license-.+'
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'

before_script:
  - echo no | android create avd --force -n test -t android-21 --abi armeabi-v7a
  - emulator -avd test -no-skin -no-audio -no-window &
  - android-wait-for-emulator
  - adb devices
  - adb shell input keyevent 82 &
  - ls
  - pwd
  
#before_deploy:
#    - cp $TRAVIS_BUILD_DIR/.keystore $HOME
#    - cd app/build/outputs/apk/
#    - jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 -keystore $HOME/alertDialog_key.jks -storepass "Quark@123" -keypass "Quark@123" app-release-unsigned.apk key0
#    # Verification
#    - jarsigner -verify app-release-unsigned.apk
#    - "${ANDROID_HOME}/build-tools/25.0.2/zipalign -v 4 app-release-unsigned.apk alertDialog.apk"
    
#deploy:
#  provider: releases
#  api_key:
#    secure: "f6203544cf8321095292148651a50acf3ed3e143"
#  file: "alertDialog.apk"
#  skip_cleanup: true
#  on:
#    repo: QKDevOpsGitHub/and_alertDialog
 
##deploy:
##   provider: releases
##   user: "QKDevOpsGitHub"
##   password: "Quark$1234"
##   file: "app/build/outputs/apk/and_dialogTest_1_0.apk"
##   skip_cleanup: true
##   on:
##     tags: false
 
deploy:
  provider: testfairy
  api-key: "aa2f450c873d306064c9e227872fab23fac448ec"
  keystore-file: bin/mykey
  storepass: Quark@123
  app-file: bin/AlertDialogTest.apk
  testers-groups: Bank_Sinarmas	
  alias: testAndroid
  